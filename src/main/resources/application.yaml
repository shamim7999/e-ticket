spring:
  application:
    name: eticket

  datasource:
    url: jdbc:postgresql://localhost:5432/eticketdb
    username: postgres
    password: Ss#01535
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update   # auto create/update tables
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect